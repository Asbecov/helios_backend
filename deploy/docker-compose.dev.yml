services:
  api:
    ports:
      - "8000:8000"
    build:
      context: .
    volumes:
      - .:/app/src/

  taskiq-worker:
    volumes:
      - .:/app/src/
    command:
      - taskiq
      - worker
      - helios_backend.tkq:broker
      - --reload

  taskiq-scheduler:
    volumes:
      - .:/app/src/
    command:
      - taskiq
      - scheduler
      - helios_backend.tkq:scheduler

  telegram-bot:
    volumes:
      - .:/app/src/
    command:
      - python
      - -m
      - helios_backend.bot

  db:
    ports:
      - "${HELIOS_BACKEND_DB_EXTERNAL_PORT:-5432}:5432"
